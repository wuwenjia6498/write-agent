# Nixpacks 构建配置
# 强制使用 Python 3.11 构建后端服务，避免被识别为 Node.js 项目

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r server/requirements.txt"]

[start]
cmd = "cd server && python main.py"
