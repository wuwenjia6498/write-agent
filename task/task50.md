# Role
前端工程师

# Task
优化“频道管理 -> 频道基本信息”页面 (如 `app/admin/channels/page.tsx` 或相关组件)，去除冗余的信息版块，合并重复项。

# UI Update Requirements
1. **删除【素材标签】版块**：找到页面右下角渲染“素材标签 (Material Tags)”区块的代码，将其彻底删除（因为底层系统已切换为无标签的 RAG 语义检索）。
2. **合并冗余卡片**：
   - 找到紧挨着头部描述下方的【目标读者】和【品牌人格】两个小卡片。
   - 删除这两个卡片的 UI。
   - 将【目标读者】的字段合并到【AI 写作人格】卡片中，作为第一个配置项（字段名可保留为 `target_audience`）。
3. **重新排版**：
   - 优化删除卡片后的页面布局，确保留下的【AI 写作人格】、【频道规则】（必须遵守/严格禁止）和【频道屏蔽词】三个大版块在视觉上更加紧凑、平衡。