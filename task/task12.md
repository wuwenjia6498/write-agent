# 任务：重构 Step 2 逻辑——打通数据链路、实现摘要预览与在线编辑

## 1. 后端修复：解决数据为 NULL 的 Bug
- **任务**：检查 Step 2 (信息搜索) 节点。确保 AI 在生成详尽调研资料后，必须将完整文本存入 `writing_tasks` 表的 `knowledge_base_data` 字段。
- **追加逻辑**：在生成全文的同时，要求 AI 额外提炼一份 300 字以内的“核心要点摘要 (Knowledge Summary)”，并存入该表的 `knowledge_summary` 字段（如无此字段请新增）。

## 2. 前端实现：双层交互视图 (Summary + Editor)
在 Writing Workbench 的 Step 2 节点，请实现以下布局：

### A. 顶部：核心要点摘要 (Summary View)
- **展示形式**：以醒目的“卡片”或“引用块”形式展示 `knowledge_summary` 字段内容。
- **作用**：让用户通过 3-5 个关键点，快速判断调研质量。

### B. 主体：调研全文本编辑器 (Full-Text Editor)
- **展示形式**：提供一个可折叠或直接展开的 Markdown 编辑器，加载 `knowledge_base_data` 的全文。
- **人工干预**：允许用户直接修改内容。根据《写作价值观》，这是主编注入“真实触感”的关键环节。

### C. 底部：确认卡点
- **动作**：增加“保存并确认调研结论”按钮。点击后，锁定当前知识库数据并触发 Step 3 (选题讨论)。

## 3. 视觉参考与体验
- 结构逻辑参考风格画像确认窗口的精简风格。
- 当调研内容过多时，确保编辑器区域有独立的滚动条，不干扰整体页面布局。

## 4. Think Aloud
- 请先说明你打算如何处理“AI 摘要”的生成时机——是在全文生成后立即触发，还是异步并行生成？