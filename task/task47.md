# Role
资深 Python/Prompt 工程师

# Task
优化 `server/services/workflow_engine.py` 中负责生成 `Step 4 (协作文档)` 的逻辑和提示词，大幅降低用户的输入负担。

# Logic Update & Prompt Optimization
1. 找到生成 Step 4 协作文档的函数（例如 `execute_step_4` 或类似步骤）。
2. **注入上下文**：确保在生成 Step 4 的提示词时，将 Step 2 检索到的 `【补充参考资料】` (即知识库内容，包含理论和微素材) 喂给大模型。
3. **重写大模型的 System/User Prompt**：
   - 告诉 AI："你现在正在为人类运营规划写作分工。请**首先评估我们已有的【补充参考资料】**。"
   - "如果资料中**已经包含**了真实的课堂案例、书籍推荐或教学步骤，AI 应主动承担将这些素材融入文章的任务，**不要再要求用户提供**。"
   - "只有当资料中极度缺乏某项核心论据（例如：完全没有具体的书名，或完全没有三年级的真实反馈）时，才在协作文档中以【可选补充】的形式向用户提问。"
   - "用户需要提供的内容必须**极度精简**，最多不超过 2-3 个简单的问题，绝不能出现长篇大论的填空题（如要求用户填写几月几号、具体天数、详细数据等）。允许用户直接回复'无'或跳过。"