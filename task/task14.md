# 任务：重构 Step 2 UI，实现摘要与全文内容的逻辑分离

## 1. 现状问题
从 `image_8c2337.jpg` 发现，上半部分的“预览窗”和下半部分的“编辑框”内容完全重复。
这增加了用户的阅读负担，且没有发挥“摘要”辅助决策的作用。

## 2. 数据绑定优化
- **上半部分 (Summary Window)**：请修改数据绑定，使其仅显示 `writing_tasks` 表中的 `knowledge_summary` 字段（如果为空，则调用 LLM 生成 300 字摘要）。
- **下半部分 (Full-Text Editor)**：继续保持与 `knowledge_base_data` 的绑定，但默认设为“折叠”状态，用户点击编辑时再展开。

## 3. 交互流程优化
- **预览模式**：上半部分以 Markdown 渲染后的精简版显示，突出“关键词”和“核心结论”。
- **编辑同步**：当用户在下半部分修改了“调研全文”并保存时，增加一个“重新生成摘要”的小按钮，方便用户同步更新上半部分的简报。

## 4. UI 调整
- 减小上半部分窗口的高度，使其不再占据过大空间，留出更多位置给下方的“需要您的确认”卡片。

## 5. Think Aloud
- 请先说明你打算如何处理 `knowledge_summary` 字段不存在旧数据时的自动生成逻辑，是否需要在后端增加一个异步的摘要提炼 Node？