# Role
资深 Prompt 工程师

# Task
解决大模型在 Step 7 (初稿生成) 时偶发的“否定指令失效（粉红大象效应）”问题，强化对全局禁用书目的拦截力度。

# Logic & Prompt Update Requirements (server/services/workflow_engine.py)
1. **定位代码**：找到 `execute_step_7` 中构建大模型 System Prompt 的逻辑。
2. **位置调整（利用近因效应）**：将关于“禁用书目”的警告，移动到整个 System Prompt 的**最末尾**（即紧贴着让大模型开始输出的那句话之前）。
3. **话术重构（强化正向约束）**：将原本单纯的“绝对禁止使用”升级为带有严重后果警告的强力指令。请使用以下结构替换原有的禁用提示词：

   """
   【⚠️ 致命操作红线（系统级校验）】
   在引用书籍案例时，你**必须且只能**从上方提供的“知识库参考资料”中挑选真实教案书目！
   如果知识库中没有合适书目，宁可泛泛而谈（如“有些经典长篇童书”），也**绝对禁止**触发以下过度引用的违禁书名：{banned_books_list}。
   注意：系统将在你生成完毕后进行硬编码级别的词表扫描。如果你的输出中出现了上述任何一个书名，将被判定为严重生成事故！
   """