# Role
资深前端工程师

# Task
修复任务历史详情页 (`app/tasks/[id]/page.tsx` 及其相关组件) 中，Step 5 和 Step 6 UI 未与主工作台 (Workbench) 同步更新的遗留问题。

# UI Update Requirements

1. **左侧流程侧边栏同步**：
   - 找到渲染历史页左侧步骤导航的代码。
   - 移除 Step 5 (风格建模) 和 Step 6 (挂起等待) 下方的“卡点”字样。
   - 将这俩步骤的副标题统一改为“自动流转”或“自动”，保持与最新版工作台一致。

2. **重构 Step 5 详情展示区 (去除全量展示与弹窗)**：
   - 在渲染 Step 5 详细内容的区域，**彻底删除**遍历展示“全部样文 (x篇)”的列表代码。
   - **彻底删除**点击样文触发弹窗 (Modal/Dialog) 试图查看样文内容的交互代码。
   - **对齐工作台 UI**：直接复用咱们在工作台 Step 5 优化的展示逻辑。读取 `task.brief_data?.selected_samples`（或后端实际存放被选中样文标题的字段）。
   - 渲染一个与工作台一致的提示卡片，文案如下：
     - **主标题**：✅ 风格基调已自动锁定
     - **正文**：创作时 AI 已从样文库中抽取了以下标杆文章进行排版与语气复刻：
     - **列表**：以简单清晰的 `<li>` 或文本形式展示被抽中的样文标题。

3. **防错处理**：
   - 如果遇到极旧的历史任务（没有 `selected_samples` 数据），则平滑降级，仅显示：“✅ 风格基调已自动锁定（旧版数据无法显示具体样文）”。