# 任务：将“风格 DNA”正式接入创作工作流 (Writing Workflow)

## 1. 逻辑联动 (Backend)
- **DNA 提取**：在发起创作任务时，根据 `channel_id` 自动提取该频道的 `style_dna` 数据。
- **S5 节点增强**：修改 Step 5 的逻辑。除了 RAG 检索素材，还必须将提取出的《风格画像》与《创作指南》作为上下文 (Context) 锁定。

## 2. 工作台 UI 升级 (Frontend)
- **S5 预览卡片**：在 Writing Workbench 的 Step 5，将 “风格画像”和“结构逻辑”以精简模式展示出来。
- **用户确认**：增加一个“确认风格并继续”的按钮，作为 S3 之后的第二个物理交互卡点。

## 3. S7 创作指令注入 (Prompt Engineering)
- 请优化 Step 7 的 Prompt 模板：
    - 强制 AI 按照 DNA 中的 [结构逻辑] 安排段落。
    - 强制 AI 执行 DNA 中的 [创作指南]（如：句式比例、标点偏好）。
    - 严禁编造素材，必须将检索到的真实感悟自然织入上述结构中。

## 4. 执行规范
- 请先输出 **Think Aloud**，说明你打算如何将 DNA 中的 10 条“创作指南”转化为 AI 能够理解的评分标准，以便在 Step 8 的审校环节进行对比检查。