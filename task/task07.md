# 任务：实现“风格画像”的可视化输出与展示

## 1. 后端逻辑增强 (Analysis Node)
- 在 Step 5 节点中，调用 LLM 对标杆样文进行 6 大维度的结构化提取。
- 输出格式：请确保 AI 输出一个 JSON 结构，包含 `style_portrait` (简短描述)、`structural_logic` (列表形式) 和 `tone_features` (关键词组)。

## 2. 前端 UI 实现
- **频道管理页更新**：在频道详情区域 增加一个“风格 DNA 看板”，展示该频道样文生成的综合画像。
- **创作工作台更新**：在 Step 5 运行完成后，在主操作区或 Think Aloud 面板中展示“本次任务风格指引”卡片。
- **内容要求**：展示结构逻辑（如：生活场景切入 -> 痛点引导）和语气特征（如：亲切、平等、书房老友式）。

## 3. 执行要求
- 请先输出你的 **Think Aloud**，展示你为这个“风格画像”设计的 UI 原型草图（可以使用 Markdown 表格或 Tailwind 类名描述）。