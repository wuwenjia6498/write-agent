# Role
资深 Prompt 架构师

# Task
深度重构 `server/services/workflow_engine.py` 中的 `execute_step_7` (初稿创作) 方法，解决大模型的“注意力稀释”和“提示词疲劳”问题。重点是优化 Prompt 的结构、层级，并利用“近因效应”将致命红线置于大模型动笔前的最后位置。

# Refactoring Requirements:

请找到 `execute_step_7` 方法，并按照以下结构彻底重写 `system_prompt` 和 `user_message` 的拼接逻辑：

## 1. 重构 System Prompt (明确角色与基调)
将 System Prompt 精简为只包含角色设定、字数要求和频道调性。移除所有具体的红线警告（稍后移至 User Message 末尾）。

```python
        system_prompt = f"""{channel_config['system_prompt']['role']}

## 🎯 你的核心任务
请全神贯注于文字的流动感和对参考样文风格的精准复刻。写出有温度、有节奏、有真实感的初稿。无需担心排版细节，稍后会有审校环节处理。

## 📊 创作规格
- 目标字数：{word_count}字（允许范围：{int(word_count * 0.9)} ~ {int(word_count * 1.1)}字）
- 频道调性：{chr(10).join(['- ' + style for style in channel_config['system_prompt']['writing_style']])}
{self._build_channel_rules_prompt(channel_config)}
"""

2. 梳理 User Message 素材注入结构
在拼接 user_message 时，确保各大知识板块（样文、调研、RAG 补充、用户反馈）之间有清晰的界限和统一的格式。

3. 终极防线前置 (近因效应的极致应用)
将原本散落在各处的【开头红线】、【高级开场策略】和【防占位致命红线】，全部移至 user_message 的最末尾（紧贴着让它开始创作的指令之前）。

请按照以下结构重写 user_message 的组装代码：
user_message = f"""请为以下选题创作文章初稿：
## 选题
{selected_topic}

## 风格指南
{style_guide}

=========================================
【你的创作弹药库（请深度融合以下素材）】
=========================================
{sample_section}{knowledge_section}{rag_section}{feedback_section}

=========================================
【🚨 动笔前的最高军规（系统级硬性校验）🚨】
=========================================
请在开始输出正文前，将以下四条军规死死刻在脑子里。系统将对你的输出进行极其严格的词表扫描，任何触碰底线的行为都将导致输出被直接废弃！

1. **【绝对禁止"无中生妈/生爸"】**：严禁使用"最近收到一位妈妈的留言"、"前几天有位家长问我"等虚构的读者提问作为开头！这种方式极度虚假。也不要用"你家孩子是不是也这样..."作为生硬引入。
2. **【强制使用高级开场白】**：请务必从以下三种高级策略中择一开场：反常识引入（直接抛出颠覆认知观点）、微观场景白描（细致描绘孩子的真实阅读状态）、直击核心概念（直接切入教育理念）。
3. **【防偷懒防占位红线】**：在需要举例时，必须且只能从上方的【补充参考资料】中挑选真实教案。绝对禁止引用《夏洛的网》、《小王子》等全网泛滥的禁用书目（{banned_books_list}）。严禁在文中写出违禁书名并加括号备注（如"注：此处为示例"），禁止使用任何形式的违禁书目作为占位符！
4. **【严禁凭空捏造】**：所有案例、故事、数据必须来自上方的参考资料，绝不允许编造。如果资料不够，请简化论述。

确认已牢记上述军规，请直接输出纯净的文章初稿：
"""