# 任务：升级“添加素材”组件，支持样文管理与文件导入

## 1. UI 逻辑调整
- **动态表单**：在 `MaterialType` 下拉框中新增“样文/风格参考”。
- **文件支持**：在素材内容上方增加“拖拽上传”区域，支持 .md 和 .docx 自动解析。
- **链接导入**：增加一个“通过链接导入”的 Tab，支持输入公众号 URL 自动抓取内容。

## 2. 字段增强
- 当选择“样文”类型时，隐藏“来源”字段，显示“风格标签”（多选：温润、逻辑、文学性、互动感）。
- 增加一个“质量权重”评分（1-5），用于 RAG 检索时优先调用高分样文。

## 3. 执行要求
- 请先输出 **Think Aloud**，说明你如何修改后端 `PersonalMaterials` 表的 Schema 以兼容样文特有的元数据。