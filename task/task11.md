# 任务：将“需要您的确认”弹窗升级为“可编辑的任务简报”

## 1. 交互升级要求
- **创作指南微调**：将 8 条硬性规则变为可编辑的输入框，允许用户添加针对本篇任务的特殊要求（例如：要求加入某位具体家长的反馈案例）。

## 2. 逻辑联动
- **参数传递**：用户在弹窗中修改后的内容，必须覆盖数据库中该任务的初始 `style_dna` 快照，并作为 Step 7 (初稿创作) 的最高指令集。

## 3. 视觉反馈
- 当用户修改了内容后，右侧“本篇风格指引”侧边栏的“一致性评分”应实时重算（或提示已人工干预）。

## 4. Think Aloud
- 请先输出你的 **Think Aloud**，说明你打算如何处理用户编辑后的数据持久化，确保如果任务中断，用户修改后的“确认方案”不会丢失。