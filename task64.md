# Role
资深 Prompt 架构师

# Task
解决大模型在 Step 8（审校阶段）产生“字数统计幻觉”的 Bug，禁止其捏造虚假字数并附加在最终文章中。

# Requirements (修改 `server/services/workflow_engine.py`):

找到 `execute_step_8` 的 System Prompt 构建区，进行以下两处修改：

1. **禁止在文章末尾附加字数统计**：
在 `## 🔵 第四遍：排版与细节审校` 区域，修改最后一句话：
将原句：“- 【重要】确保总字数在 {min_allowed}~{max_allowed} 字范围内”
**修改为**：
“- 【重要】请凭借你的内容把控力，通过删减冗余使文章体量尽量贴近目标要求。**绝对禁止**在最终修改后的文章标题或末尾自己捏造并标注'全文共xxx字'、'修改后版本（xxx字）'等字眼！只输出纯净的正文即可。”

2. **简化审校报告中的字数汇报（不强制其计数）**：
在 `### 审校报告` 的 `#### 第四遍：排版与细节审校` 格式要求中：
将原句：“- 字数：当前 xxx 字（{"需要精简" if is_over_limit else "符合要求"}）”
**修改为**：
“- 冗余精简：已删除/精简了 xxx 等冗长表述（若无严重超标则填'体量适中，正常润色'）”
