# Role
全栈开发工程师 (AI 交互与前端体验专家)

# Task
为工作流的最终输出环节（Step 8 之后的文章展示区）开发“划词 AI 重写（Inline AI Rewrite）”功能。实现类似 Notion AI 的极致人机共创体验。

# Requirements

## 1. 前端交互重构 (Editor UI)
- 将最终文章的展示区域从普通的 Markdown 渲染，升级为支持选区的富文本展示组件（建议使用标准 React 选区状态管理或轻量级方案）。
- **交互逻辑**：当用户在文章中划选（Select）一段文本时，在选区附近弹出一个优雅的悬浮浮层（Floating Toolbar / Popover）。
- **浮层内容**：包含一个单行输入框（Placeholder："告诉 AI 怎么修改这段话，例如：换一个更儿童化的大师名言..."）和一个带有魔法棒图标的提交按钮。

## 2. 后端 API 扩展 (server/services/ai_service.py 或新建路由)
- 新增一个专门处理“局部重写”的 API Endpoint（如 `/api/ai/inline-rewrite`）。
- **入参格式**：
  - `selected_text`: 用户划选的文本。
  - `surrounding_context`: 选区前后的上下文（各取 200 字即可，用于保证逻辑连贯）。
  - `user_instruction`: 用户在悬浮框里输入的修改指令。
  - `channel_id`: 当前频道的 ID（用于保持语气一致性）。

## 3. 核心 Prompt 组装 (局部微创手术)
- 在后端接收到请求后，组装以下 System Prompt 调用 LLM：
  """
  你是一个顶尖的文字编辑，正在对文章进行局部微调。
  【用户要求】：{user_instruction}
  【需要修改的原文】：{selected_text}
  【原文所在的上下文】（仅供参考连贯性，不需要输出上下文）：{surrounding_context}
  
  【你的任务】：
  严格按照用户的要求，重写【需要修改的原文】。
  【最高红线】：
  1. 你只能输出重写后的文本，**绝对不能**输出任何多余的解释、寒暄、或者包含上下文的句子！
  2. 确保你输出的文本，能够完美、丝滑地嵌回【原文所在的上下文】中，不能有任何语病或逻辑断裂。
  3. 保持温润、专业的语气。
  """

## 4. 前端替换更新
- 收到后端的流式或一次性返回后，将界面上被划选的文本平滑替换为 AI 生成的新文本，并给用户一个微小的成功反馈（如绿色的渐隐高亮背景色提示）。